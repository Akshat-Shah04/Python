# What happens when „1‟== 1 is executed?
print("1" == 1)
# False


"""
"1" is a string and 1 is an integer. Since their types are different, Python does not attempt to convert them to a common type and directly determines that they are not equal.
So, "1" == 1 evaluates to False.
"""
